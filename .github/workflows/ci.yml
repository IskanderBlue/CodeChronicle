# # turning off for now
# name: CI

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# jobs:
#   lint:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.12"
#       - name: Install ruff
#         run: pip install ruff
#       - name: Lint
#         run: ruff check .

#   test:
#     runs-on: ubuntu-latest
#     services:
#       postgres:
#         image: postgres:16
#         env:
#           POSTGRES_DB: codechronicle_test
#           POSTGRES_USER: postgres
#           POSTGRES_PASSWORD: postgres
#         ports:
#           - 5432:5432
#         options: >-
#           --health-cmd pg_isready
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5
#     env:
#       DATABASE_URL: postgres://postgres:postgres@localhost:5432/codechronicle_test
#       DJANGO_SETTINGS_MODULE: code_chronicle.settings.development
#       ANTHROPIC_API_KEY: test-key-not-real
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-python@v5
#         with:
#           python-version: "3.12"
#       - name: Install dependencies
#         run: pip install -e ".[dev]"
#       - name: Run migrations
#         run: python manage.py migrate --no-input
#       - name: Run tests
#         run: pytest -v --tb=short
