# 6.1 — Engagement / Click Tracking

## Status: implemented and fully verified — human click-through confirmed (2026-07-24); one follow-up open (orphaned viewer overlay)

## What

Add server-side event tracking for what users *do with* search results —
which provision versions, regulation clauses, and result links they open —
to complement the search-input tracking we already capture in
`SearchHistory`.

## Why

Today we record every search's inputs (`SearchHistory.parsed_params` holds
keywords, date, province, building_type) and `result_count`/`top_results`,
but we have **no record of engagement**: no model logs that a user opened a
particular `CodeEditionProvisionVersion`, drilled into a `RegulationClause`,
or followed a link out of a results list. The detail views
(`core/views/regulation.py:regulation_detail`, `provision_permalink`) just
render templates. Without this we can measure demand (what people look for)
but not value delivered (what they actually read), and we can't compute
click-through rate per query.

## Design

### New model: `EngagementEvent` (core)

A single thin, append-only event table — one row per tracked interaction.

| Field | Type | Purpose |
|---|---|---|
| `user` | FK→User, null=True | who (authed) |
| `ip_address` | GenericIPAddressField, null=True | who (anonymous) |
| `event_type` | CharField(choices) | `provision_version_view`, `regulation_clause_view`, `result_link_click` |
| `object_type` | CharField | model label of the target, e.g. `CodeEditionProvisionVersion` |
| `object_id` | BigIntegerField, null=True | target pk (kept loose — not a hard FK, see Notes) |
| `search` | FK→SearchHistory, null=True | the search this engagement came from, when known |
| `context` | JSONField(default=dict) | free-form (provision_id, division, reg_id, clause_id, source surface) |
| `timestamp` | DateTimeField(auto_now_add) | when |

Indexes: `(event_type, timestamp)`, `(object_type, object_id)`,
`(search,)`. `db_table = "engagement_events"`.

Rationale for a single generic table over per-target tables: the event
shapes are near-identical and we want cheap "top N most-viewed versions /
clauses" aggregates without UNIONing. Mirrors how `SearchHistory` already
carries heterogeneous detail in a JSONField.

### Capture paths

Two complementary mechanisms:

1. **Server-rendered view hits** — in `regulation_detail`,
   `provision_permalink`, and the provision-version render path, write an
   `EngagementEvent` (non-fatal `try/except`, matching the
   `search_service.py:74` pattern). This catches direct/permalink navigation
   with zero client JS.

2. **Result-link clicks** — a lightweight beacon endpoint
   `POST /api/event` (Django Ninja, in `api/`). The results templates are
   already HTMX-driven, so wire result links with an `hx-post` (or a small
   `fetch` on click) carrying `{event_type, object_type, object_id,
   search_id, context}`. The `search_id` ties the click back to the
   originating `SearchHistory` row so click-through rate per query is
   computable. Keep the search id in the rendered results so the beacon can
   echo it back.

### Privacy / rate-limit interaction

- Reuse the existing anonymous-vs-authed identity split (`user` else
  `ip_address`) already used by `SearchHistory` and `RateLimitMiddleware`.
- The beacon endpoint must NOT count against search rate limits — it is not
  a search. Confirm `RateLimitMiddleware` only gates the search path; add an
  exemption if it gates by path prefix.

## Changes

- `core/models.py`: add `EngagementEvent` + migration.
- `core/views/regulation.py`: record view events in `regulation_detail` and
  `provision_permalink` (non-fatal).
- Provision-version render path (search/viewer): record version-view events.
- `api/`: add `POST /api/event` beacon endpoint (validate payload, write
  event, return `{"success": true}` envelope per CLAUDE.md API contract).
- Results/detail templates: emit `data-*`/`hx-post` so links fire the beacon;
  thread the `search_id` through to results so clicks attribute to a search.
- `core/admin.py`: register `EngagementEvent` read-only for inspection.

## Completion Conditions

- Opening a provision version, regulation detail, or provision permalink
  writes exactly one `EngagementEvent` of the right `event_type`.
- Clicking a result link fires the beacon and writes one event carrying the
  originating `search_id`.
- Event writes are non-fatal: a tracking failure never breaks the page or
  the search.
- Beacon endpoint is exempt from search rate limiting.
- Anonymous and authenticated events both record correctly (ip vs user).

## How to Test

### `engagement_event_written_on_provision_version_view`
- Request a provision permalink → assert one `EngagementEvent` with
  `event_type="provision_version_view"` and matching `object_id`.

### `engagement_event_written_on_regulation_detail`
- GET `regulation_detail` → assert a `regulation_clause_view` /
  `regulation_view` event row.

### `beacon_endpoint_records_result_click_with_search_link`
- POST `/api/event` with a valid payload including `search_id` → 200,
  `{"success": true}`, and an `EngagementEvent` linked to that
  `SearchHistory`.

### `tracking_failure_is_non_fatal`
- Force the event write to raise (patch `.objects.create`) → the page/search
  still returns 200.

### `beacon_not_rate_limited`
- Exhaust the search rate limit, then POST `/api/event` → still succeeds.

## Depends On

- `SearchHistory` (exists) — for the `search` FK and click-through joins.
- The search results render path that exposes `top_results` / result ids —
  needs the `SearchHistory` id surfaced into the results template.

## Notes

- `object_id` is intentionally a loose `BigIntegerField`, not a hard FK:
  events outlive the rows they point at (re-loads via `load_edition` replace
  provision/version pks), and an append-only analytics log should not cascade
  or block deletes. Resolve targets at report time, tolerating misses.
- Out of scope for this task: dashboards/reporting UI, GA/Plausible/PostHog
  or any third-party client analytics, and aggregation materialized views.
  This task only lands the capture layer. A follow-up (`6.2`) can add
  reporting (e.g. top-viewed provisions, CTR per keyword) once data exists.
- Follow the repo terminology: events about generic structural units use
  "provision", not "section".

## Implementation Update (2026-06-02)

Landed the capture layer. Files:

- `core/models.py`: `EngagementEvent` model (`engagement_events` table) +
  migration `0023_engagementevent.py`. `search_id` FK id-shadow declared for
  Pyright, matching the repo convention.
- `core/events.py` (new): `record_event(request, …)` — best-effort, swallows
  failures, derives user-vs-IP identity the way the search path does, and
  coerces a string `search_id` (drops it if not a positive int).
- `core/views/regulation.py`: `regulation_detail` records `regulation_view`;
  `provision_permalink` records `provision_version_view` pinned to the linked
  version.
- `core/views/search.py`: `viewer_section_content` records a
  `provision_version_view` for the matched provision's in-force version — this
  is the **primary search-result click-through** capture, server-side, no
  client beacon needed.
- `services/search_service.py` → `search.py` view → `search_results_partial.html`
  → `search.html`: threads `search_history_id` so the viewer drill-in
  attributes back to its originating search (stashed in `#cc-search-id`, read
  by `sectionContentUrl()`).
- `api/views.py`: `POST /api/event` beacon (public, paid-access guard
  deliberately skipped) for out-of-band clicks (external source links, PDF
  downloads). Validates `event_type` against the model's choices.
- `core/admin.py`: read-only, add-disabled `EngagementEventAdmin`.
- `core/tests/test_engagement.py`: 8 tests — all green.

### Deviations from plan

- Used `regulation_view` (not `regulation_clause_view`): the detail page is a
  whole regulation, not a single clause.
- The main result→provision click-through is captured **server-side** in the
  existing `viewer_section_content` round-trip rather than via a client
  beacon. More robust and directly testable; the `/api/event` beacon remains
  for clicks with no server round-trip.

## Beacon Wiring Update (2026-06-02)

Wired the `/api/event` beacon to the two in-app links that leave without a
server round-trip, both in the search viewer overlay (`templates/search.html`):

- **Source link** (citation → e-Laws/source_url): Alpine `@click="recordLinkClick('source_link')"`.
- **PDF download** (`pdf_download_url`): the anchor lives inside a JS-cloned
  `<template data-pdf-picker-template>`, outside Alpine's reactive DOM, so
  `@click` can't bind. Marked it `data-beacon-pdf` and added a delegated
  document click listener that routes through the overlay component's
  `recordLinkClick()` via the existing `withViewerOverlay()` helper.
- New `recordLinkClick(surface)` method POSTs `result_link_click` to
  `/api/event` with `keepalive: true` (survives the new-tab navigation),
  `credentials: 'same-origin'` (session cookie → user attribution), and the
  stashed `#cc-search-id` for click-through attribution. Wrapped + `.catch()`
  so a tracking failure never blocks the click.

Gotcha avoided: the inline `{# … #}` note on the PDF link is kept on ONE line
— a multi-line Django comment tag leaks as literal page text (project memory).

## Open Issues

- ~~Live browser click-through not yet exercised.~~ Done 2026-07-23 — see
  "Live Verification" below.
- Reporting/dashboards remain out of scope (future `6.2`).

## Gap-Closing Update (2026-06-02)

Live testing revealed the real gap: the primary "open a provision" action is
**expanding the result accordion inline in the results list**, which is a pure
Alpine show/hide with **no server round-trip** — so nothing was recorded. (The
viewer-overlay path that *does* hit the server is a separate, less-used action;
and this edition has no PDFs, so the PDF early-return theory was moot.)

- **Inline result-expand now beacons** (`window.ccRecordView` →
  `provision_version_view`, `surface: results_expand`, `search_id` attached).
  Wired via an `open()`/`toggleChild()` method on the results-partial Alpine
  `x-data`; children record only on expand (not collapse). `object_id` is null
  — the client doesn't know the version pk — with `code`/`provision_id`/
  `division` in context for report-time resolution.
- **Shared poster** `window.ccPostEvent` in `search.html` (auto-attaches the
  stashed `#cc-search-id`); the viewer's `recordLinkClick` now uses it too.
- **Gap #1 closed** via `decorateResultLinks()`: a post-render pass over
  `#results-area` that appends `?search_id=` to `/provision/…` and
  `/regulation/…` links, so permalink/clause click-throughs attribute to the
  search through the existing server-side capture (no double count).

Gotcha hit & recorded to memory: a `{% url %}` written **inside a JS comment**
in a `.html` template still gets compiled by Django (text-based parser) →
argument error, page 500. Reworded the comment.

## Verification Run

- Passed: `pytest core/tests/test_engagement.py` (8)
- Passed: `pytest core/tests/test_regulation_views.py api/tests/test_integration.py core/tests/test_middleware.py` (21)
- Passed: `ruff check` on all changed files.
- Passed: `manage.py check` (0 issues).
- Passed: rendered `/` (search page) — all beacon wiring present, no leaked
  `{#`/`{%` template tags.

## Live Verification (2026-07-23)

Ran against the dev corpus (9,750 provisions) in a real browser. Every row the
run created was removed afterwards; the dev DB is back to its prior 337 events /
574 searches.

### Surface census — what had actually ever fired

Aggregating the 337 pre-existing events by `(event_type, context.surface)`:

| surface | events | attributed to a search |
|---|---|---|
| `permalink` | 185 | 69 |
| `results_expand` | 93 | 93 |
| `regulation_view` | 59 | 25 |
| `search_viewer` | **0** | — |
| `result_link_click` | **0** | — |

So three surfaces were already proven in production-like use — including
`decorateResultLinks()` (the 69 + 25 search-attributed permalink/regulation
views can only come from a decorated link) and the gap-closing
`results_expand` beacon. Two had never fired even once.

### `search_viewer` — works, but unreachable from the UI

Driving `viewer_section_content` directly against real OBC 2006 data records
correctly (right surface, right version pk, right `search_id`). It is not
broken. It has never fired because **nothing in the results UI can open the
viewer overlay**: `search.html` listens for `[data-viewer-open]`, but no
template emits that attribute — `grep -rn data-viewer-open templates/` returns
only the listener itself. The emitting markup went away when the results UI was
rebuilt around the inline accordion (`git log -S` points at 1601123 / 88f1067).

Consequence: the overlay Alpine component, `openViewer()`, `sectionContentUrl()`,
and the overlay's `recordLinkClick('source_link')` / `pdf_download` beacons are
all currently unreachable. **Deliberately not deleted** — the same judgement as
the BYOD PDF viewer: this looks like UI that is meant to come back, and the
capture wiring inside it is correct. Decide its fate before removing anything.

### `result_link_click` — real gap, now closed

The card's claim that the source link is wired was true only of the *overlay's*
copy, which no user can reach. The source links a user can actually click live
in the results list (42 on a 10-result page: 20 "consolidation" + 22 "ext").
None of them was beacon-wired, so the surface was uncapturable in practice.

Added a delegated handler in `search.html` scoped to off-origin links inside
`#results-area` (internal links keep to `decorateResultLinks()`, so nothing is
double-counted). `object_id` is null with `href`/`label` in context — the client
has no pk for an e-Laws URL, same shape as `ccRecordView`.

**Registered on the capture phase, and that is load-bearing.** 22 of the 42
links carry Alpine's `@click.stop`; a bubble-phase document listener never sees
them, so a naive version of this fix would have silently captured only half the
clicks. Caught in the browser, not by reasoning about the template.

Verified end to end: a click on a `@click.stop` "ext" link writes
`result_link_click / surface=source_link / search_id=601 / label=ext` with the
e-Laws href. `core/tests/test_engagement.py::TestSourceLinkBeaconWiring` guards
the capture flag against a silent regression.

### Content gate interaction (new since the flag removal)

`FREE_TIER_GATING_ENABLED` is gone and the gate is now unconditional, which
changes what engagement data exists: for an anonymous or free user hitting a
non-free edition, `provision_permalink` / `regulation_detail` return 403 and
`viewer_section_content` returns a locked teaser — **none of them records an
event**. Two consequences worth a decision (not made here):

1. Click-through metrics for non-Pro users now describe free-tier content only.
2. ~~"User hit a paywalled provision" is not recorded at all.~~ **Closed** — see
   "Locked-content capture" below.

This also broke five engagement tests (fixtures build an OBC 1997 edition and
hit it anonymously). Fixed by scoping the fixture edition into the free tier for
the duration of the test, so engagement coverage stays independent of whichever
edition happens to be free.

### Verification method note

Coordinate-based clicks from the browser automation never dispatched into the
page (a document-level capture probe saw zero click events from three separate
attempts), so the final click was dispatched as a bubbling `MouseEvent` on the
real link in the real page. That exercises the capture-vs-`stopPropagation`
interaction — the actual risk — but does leave "a trusted user click reaches the
handler" resting on the DOM spec rather than on an observed trusted click.

## Locked-content capture (2026-07-23)

Every other event type records value *delivered*; this one records value
*withheld*. Added `EngagementEvent.EventType.LOCKED_CONTENT_VIEW`
(migration `0042_engagement_locked_content_view`), one kind distinguished by
`context.surface` — the same convention `provision_version_view` already uses
across permalink / search_viewer / results_expand.

| surface | where | kind of signal |
|---|---|---|
| `search_results` | `core/views/search.py:search_results` | impression — the search returned results this tier can't open |
| `permalink` | `_locked_edition_response` | explicit attempt on a named provision |
| `regulation_detail` | `_locked_edition_response` | explicit attempt on a named regulation |
| `edition_chain` | `_locked_edition_response` | explicit attempt on an edition timeline |
| `search_viewer` | `core/views/search.py:viewer_section_content` | explicit drill-in, refused |

Recorded inside `_locked_edition_response` rather than at its three call sites:
it is the single choke point for the gated page views, so a future gated view
gets the signal for free by using the helper. It takes a `surface` kwarg because
it can't tell which view refused.

**The `search_results` impression is the important one, and it was nearly
missed.** Locked editions render as a *count*, not as clickable rows
(`partition_results` returns `{code_name: count}`), so an in-app free user never
reaches the four 403/teaser surfaces — those need a shared permalink or a
cross-edition link to hit. Wiring only the refusal sites would have repeated the
exact mistake found earlier in this card: instrumenting a surface users can't
reach. One row per search carries the per-edition counts and attributes to the
`SearchHistory` row, so a report can join demand (the query) to what was
withheld.

Deliberately *not* recorded: `provision_lineage`'s per-link `locked` flag. That
marks rail links as Pro-only during render; it fires on every result with a
cross-edition lineage and would drown the signal in impressions nobody acted on.

Verified against the real dev corpus: an anonymous request for OBC 2012 (outside
the OBC 2006 free window) records `locked_content_view / permalink` and
`locked_content_view / search_viewer`. `TestLockedContentTracking` covers all
five surfaces plus the two negatives (an allowed edition records no locked
event; a non-htmx viewer GET is not counted, matching the view-capture rule).
Probe rows removed; dev DB back to 337 events.

### Follow-up for 6.2 (reporting)

The join that makes this worth having: `locked_content_view` rows carry
`search_id`, so per-query conversion pressure is computable — which searches
most often return content the user can't open, broken down by edition. That is
the ranked list of what to unlock or price next.

## Runbook — manual click verification (DONE 2026-07-24)

**Result: PASS.** A real human mouse click on the `@click.stop` source link
(`ext` → `ontario.ca/laws/regulation/r06350`) produced exactly one
`EngagementEvent` — `result_link_click | surface source_link | label ext |
href …/r06350`, joined to its search via `search_id`, `object_id` null (the
document-level beacon has only the href, by design). A capture-phase probe
(`window.__ccClicks`) independently confirmed the click reached the page, so
this is a genuine pass, not a synthetic-injection artefact. Baseline restored
after (337 events / 577 searches). This closes the last gap: the capture phase
survives `@click.stop`, verified against the hardest case on the page.

The runbook below is retained for re-verification.

The one thing not verified by an observed *human* click: that a real mouse click
on a `@click.stop` source link reaches the capture-phase beacon. Everything else
about that path is verified (see "Live Verification"). This runbook exists
because the browser-automation tool stopped delivering synthetic mouse clicks
into the page, so the last step needs a person.

### Setup

```bash
docker-compose up -d                      # Postgres (container: code_chronicle_db, :5432)
source venv/Scripts/activate

# Kill EVERY runserver first — a stale one keeps port 8000 and silently serves
# cached templates, which will make a correct change look broken.
#   PowerShell: Get-CimInstance Win32_Process -Filter "Name='python.exe'" |
#     Where-Object { $_.CommandLine -like '*runserver*' } | Stop-Process -Force
python manage.py runserver 8000 --noreload
```

Confirm the capture-phase handler is actually being served before touching the
browser — this catches the stale-template trap in one command:

```bash
curl -s http://127.0.0.1:8000/ | grep -c "CAPTURE phase"   # must print 1
```

### Free the search quota

`RATE_LIMIT_ANONYMOUS = 1` per IP per day (`settings/base.py:205`, not
env-backed), so a second search from 127.0.0.1 the same day is refused with no
results. Clear the day's rows first:

```bash
python -c "
import django, os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','code_chronicle.settings.development'); django.setup()
from core.models import SearchHistory
from django.utils import timezone
q = SearchHistory.objects.filter(ip_address='127.0.0.1', timestamp__date=timezone.now().date())
print('clearing', q.count()); q.delete()"
```

### The search

Query `fire separation between dwelling units`, **AS-OF `2010-06-01`**. The date
matters: it lands inside OBC 2006, the only edition in the free tier, so an
anonymous browser gets openable results. A 2024 date returns locked teasers with
no clickable source links. Expect 10 results and 42 external links.

### What to click

The source links live in `.vrail hidden @xl:block` — a *container* query, so
they only render above the `@xl` breakpoint. In a narrow window they exist in
the DOM with a zero-size box and cannot be clicked.

- **Wide window**: the `consolidation` links (20 of them) are visible. These do
  NOT stop propagation — a weaker test.
- **Any width**: the **`ext`** link in the first result (labelled "View on
  e-Laws", `https://www.ontario.ca/laws/regulation/r06350`) is visible. This one
  **does** carry `@click.stop` — it is the case worth testing. Prefer it.

It opens ontario.ca in a new tab; that is expected and harmless.

### Verify

```bash
python -c "
import django, os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','code_chronicle.settings.development'); django.setup()
from core.models import EngagementEvent
e = EngagementEvent.objects.order_by('-id').first()
c = e.context or {}
print(e.event_type, '|', c.get('surface'), '| search=', e.search_id, '| label=', c.get('label'), '|', c.get('href'))"
```

Pass looks like:
`result_link_click | source_link | search= <id> | label= ext | https://www.ontario.ca/laws/regulation/r06350`

### Clean up

Delete the probe search and its events afterwards. Dev-DB baseline as of
2026-07-23 is **337 engagement events / 574 searches** — restore to that.

### Known environment traps (all hit during this card)

- **Stale test database.** `pytest` fails with `database "test_code_chronicle"
  already exists` / `is being accessed by other users`, showing as mass ERRORs
  that look like code failures. Fix:
  `docker exec code_chronicle_db psql -U postgres -d postgres -tAc "SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE datname='test_code_chronicle' AND pid <> pg_backend_pid();"`
  then `pytest --create-db`. Do not reach for `--reuse-db` — an interrupted run
  leaves the DB half-migrated and reuse then fails on a duplicate table.
- **`Software caused connection abort` during test setup** — Docker port-forward
  flakiness under load, not a code failure. Re-run; if persistent,
  `docker compose up -d --force-recreate` (per project memory, not `restart`).
- **Templates do not hot-reload**, and `--noreload` means the process won't
  restart itself. Every template edit needs a full server restart.
